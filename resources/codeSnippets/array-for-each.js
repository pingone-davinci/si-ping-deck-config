// Iterate through each element in the array
__myArrayOfObjects__.forEach((element, index) => {
    // Output the value of the 'type' property for the current element
    console.log(element.type);

    // Output the index of the current element (0, 1, 2, etc.)
    console.log(index);
});
